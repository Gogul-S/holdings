import {StyleSheet} from 'react-native';
import {COLOR_PALLETE} from '../../theme/colorPallete';

export const holdingListStyle = StyleSheet.create({
  listStyle: {flex: 1, backgroundColor: COLOR_PALLETE.BACKGROUND_DARK},
});
